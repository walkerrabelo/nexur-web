import { RouteFriendlyPipe } from './route-friendly.pipe';

describe('RouteFriendlyPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteFriendlyPipe();
    expect(pipe).toBeTruthy();
  });
});
