import { IntervalDatePipe } from './interval-date.pipe';

describe('IntervalDatePipe', () => {
  it('create an instance', () => {
    const pipe = new IntervalDatePipe();
    expect(pipe).toBeTruthy();
  });
});
